<Window x:Class="Cyber_Espace_Entrainement.Views.Activite.Captcha.CaptchaFacileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cyber_Espace_Entrainement.Views.Activite.Captcha"
        xmlns:vm="clr-namespace:Cyber_Espace_Entrainement.ViewModels.Activite.Captcha"
        mc:Ignorable="d"
        Title="CaptchaFacileView" 
        SizeToContent="Height"
        Height="700" 
        Width="1000">
    <Window.DataContext>
        <vm:CaptchaFacileViewModel/>
    </Window.DataContext>

    <Grid>
        <!--#region ViewModel -->

        
        
       

        <!--#endregion-->

        <!-- Captcha En tete -->

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Border 
                Height="100" 
                Background="#FF2D2D30" 
                VerticalAlignment="Center">
            <TextBlock Text="Captcha Facile" 
                       Foreground="White" 
                       FontSize="36" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center"/>
        </Border>


        <!--#endregion-->

        <!-- Captcha Intro -->
        <StackPanel>
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Intro}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <Button Command="{Binding RefreshCommand}"
                x:Name="RefreshButton" 
                    Content="hop" 
                    HorizontalAlignment="Left" 
                    Margin="10" 
                    VerticalAlignment="Top" 
                    Width="75" 
                    Height="30" />
        </StackPanel>


        <!-- Captcha Jeu 1 -->
        <StackPanel>
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding JeuCaptcha}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            
            
            <CheckBox x:Name="CaptchaCheckBox" 
                Content="I'm not a robot" 
                HorizontalAlignment="Left" 
                Margin="10" 
                VerticalAlignment="Top" 
                RenderTransformOrigin="0.5,0.5" 
                Height="71" Width="272" 
                IsChecked="False" 
                Cursor="Hand" Grid.Row="1" 
                VerticalContentAlignment="Center" 
                BorderBrush="#FF707070" 
                Checked="CaptchaCheckBox_Checked">
                <CheckBox.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-360.351"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </CheckBox.RenderTransform>
            </CheckBox>
        </StackPanel>
        

        <!-- Captcha Jeu 2 -->
        <StackPanel>
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding JeuCaptcha2}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>



            <Slider x:Name="CaptchaSlider" 
            Margin="403,49,0,0" 
            Grid.Row="1" 
            Width="346" Height="109" Cursor="Hand" 
            VerticalAlignment="Top" 
            HorizontalAlignment="Left" 
            RenderTransformOrigin="0.5,0.5">
                <Slider.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleX="-0.225"/>
                        <RotateTransform/>
                        <TranslateTransform X="-52.996"/>
                    </TransformGroup>
                </Slider.RenderTransform>
            </Slider>

        </StackPanel>

        <!-- Captcha Outro -->
        <StackPanel>
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Outro}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

        </StackPanel>
        









    </Grid>
</Window>
